# Pending methods.html cards

Cards built by in-flight build agents (GLM + Qwen tracks), to be inserted into
`math-lab/engines/numerical/methods.html` in one consolidation pass after both
tracks finish.

**Status:** Both tracks are fully consolidated as of 2026-07-20 — `methods.html` now
shows all 29 methods (5 original + 14 GLM + 10 Qwen), correctly numbered 1/29–29/29.
Nothing pending. If a new method is built later, append its card snippet here first,
then do a fresh consolidation pass across all cards' `.engine-index`/`transition-delay`.

## Statistics Engine

Cards built by in-flight Statistics Engine build agents, to be inserted into
`math-lab/engines/statistics/methods.html` in one consolidation pass. Indices below are
TODO markers — the consolidator should renumber every Statistics Engine card's
`.engine-index` and `transition-delay` once all in-flight builds land.

**Status:** Fully consolidated as of 2026-07-21 — `methods.html` now shows all 11
methods (5 from the GLM track + 1 Qwen item 04 + 5 items 05–09), correctly numbered
1/11–11/11 with sequential `transition-delay`s (.08s increments). Nothing pending. If a
new Statistics method is built later, append its card snippet here first, then do a fresh
consolidation pass across all cards' `.engine-index`/`transition-delay`.
