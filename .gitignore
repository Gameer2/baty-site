# Course reference material — copyrighted, kept local only
reference/

# Curriculum reference PDFs (Jordan MoE + Cambridge) — copyrighted source
# textbooks/syllabi, kept local only. Extracted text/ mirror stays tracked.
docs/curriculum-references/**/*.pdf

# Archived pre-redesign build + unrelated template (large binaries, superseded)
archive/*
!archive/docs/

# ...but archived planning docs (small text, real git-history value) stay tracked


# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.superpowers/

# Stray screenshots
Screenshot from *.png

# Python bytecode cache
__pycache__/
*.pyc
